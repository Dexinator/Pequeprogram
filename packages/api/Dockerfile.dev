# Usar una imagen base de Node.js
FROM node:20-alpine

# Establecer el directorio de trabajo dentro del contenedor
WORKDIR /app

# Instalar dependencias nativas necesarias para paquetes como bcrypt y netcat para verificación de redes
RUN apk add --no-cache python3 make g++ netcat-openbsd

# Copiar el script de inicialización primero
COPY init-db.sh /usr/local/bin/init-db.sh
RUN chmod +x /usr/local/bin/init-db.sh

# Copiar todos los archivos del proyecto (incluye package.json, tsconfig.json y src/)
COPY . .

# Instalar dependencias - npm install normal funcionará porque src/ ya existe
RUN npm install

# Usar el script de inicialización como punto de entrada
ENTRYPOINT ["/usr/local/bin/init-db.sh"]

# Comando para ejecutar la aplicación en modo desarrollo
CMD ["npm", "run", "docker:dev"] 