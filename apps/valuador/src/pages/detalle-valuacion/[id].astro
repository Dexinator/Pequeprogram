---
import MainLayout from '../../layouts/MainLayout.astro';
import DetalleValuacion from '../../components/DetalleValuacion.jsx';

// En modo servidor completo, todas las rutas son dinámicas por defecto

// Obtener el ID de los parámetros
const { id } = Astro.params;
---

<MainLayout title={`Detalle de Valuación VP-${id}`}>
  <DetalleValuacion valuationId={id} client:load />
</MainLayout> 